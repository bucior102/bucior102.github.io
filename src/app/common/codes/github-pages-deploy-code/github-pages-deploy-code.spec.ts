import { GithubPagesDeployCode } from './github-pages-deploy-code';

describe('GithubPagesDeployCode', () => {
  it('should create an instance', () => {
    expect(new GithubPagesDeployCode()).toBeTruthy();
  });
});
